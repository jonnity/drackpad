`ort` (ONNX Runtime) を採用し、リスクを最小限に抑えつつ着実に進められるIssueリストに再構成しました。

特に、MediaPipe SDKの代わりに `ort` を使うことで、**「前処理（画像加工）」と「後処理（座標のデコード）」を自分で制御する必要がある**ため、その点をIssueとして明確に分離しています。

---

## Milestone 0: スパイク (Technical Proof of Concept)
RustからONNXモデルを叩き、指の座標を取り出せるか最速で検証します。

### Issue #0: [Spike] `ort` を用いた指先座標抽出の検証
* **内容:** 手のランドマークを検出する `.onnx` モデルを読み込み、1枚の画像から指先の座標を取得する。
* **タスク:**
    * `ort` クレートの導入とモデルファイル（`hand_landmark.onnx` 等）の用意。
    * 画像をモデル入力サイズ（例: 224x224）の `ndarray` に変換する処理。
    * `session.run` を実行し、出力された配列から人差し指の先端座標を特定する。
* **DoD:** 任意の画像に対して、指先の $(x, y)$ 座標が標準出力に正しく表示される。

---

## Milestone 1: 基盤構築 (Vision Pipeline)
カメラ映像をトラックパッドの座標系に変換するパイプラインを構築します。

### Issue #1: OpenCV キャプチャとデバッグ用 UI の構築
* **内容:** `opencv` クレートでのカメラ制御と `egui` でのデバッグ画面作成。
* **DoD:** カメラ映像がウィンドウに表示され、その上にFPSなどのステータスがオーバーレイ表示される。

### Issue #2: ホモグラフィ変換による操作エリアの定義
* **内容:** 斜め俯瞰の映像を正対化（長方形化）する。
* **タスク:**
    * 映像内の紙の4隅をクリックで指定するキャリブレーション機能。
    * 射影変換行列（`get_perspective_transform`）の算出。
* **DoD:** 斜めに置いた紙が、デバッグ用ウィンドウ内で歪みのない「真上からの長方形」として表示される。

### Issue #3: [重要] 推論と座標投影の統合
* **内容:** `ort` の推論結果を、Issue #2 で作成した平面座標系に投影する。
* **タスク:**
    * 毎フレームの推論実行。
    * **検証:** 投影後のデバッグ映像上で、自分の指先を追従する赤いドットを表示。
* **DoD:** 紙の四隅を指でなぞったとき、デバッグ映像内の対応する四隅とドットが一致する。

---

## Milestone 2: MVP (Movement Only)
マウスカーソルを動かせる状態にし、実用性を検証します。

### Issue #4: 移動のスムージングとマウスエミュレーション
* **内容:** 座標をOSのカーソル移動に変換し、ガタつきを抑える。
* **タスク:**
    * `enigo` を用いたカーソル操作の実装。
    * **低パスフィルタ / カルマンフィルタの導入:** AI推論特有の「座標の跳ね」を抑制する。
* **DoD:** 指の動きに合わせてマウスカーソルが滑らかに（違和感なく）追従する。
* **★ MVPゴール:** ここで「トラックパッド（移動）」として一旦完成。

---

## Milestone 3: MVP+1 (Click Gesture)
タップ操作を追加し、マウスを完全に代替します。

### Issue #5: 影の解析による接触検知（Touch Down/Up）
* **内容:** 指先と影のユークリッド距離を算出する。
* **タスク:**
    * 指先座標周辺のピクセルを解析し、最も暗い点（影）の座標を特定。
    * 距離が閾値以下になった瞬間のイベント（TouchDown）の検知。
* **DoD:** 10回指で紙を叩き、デバッグログに10回正確に「Touch/Release」が記録される。

### Issue #6: タップ/スライド判定ステートマシン
* **内容:** 「ただの移動」と「クリック」を物理的挙動で切り分ける。
* **タスク:**
    * 接触時の座標と時間を保持し、離した瞬間に「一定時間内」かつ「移動距離が微小」ならクリックを発火。
* **DoD:** 30秒間のカーソル移動中に誤クリックが0回、かつ意図的なタップが80%以上の精度でクリックとして認識される。

---

## Milestone 4: システム化 (Production Ready)
日常的に使えるツールに仕上げます。

### Issue #7: 常駐化機能と設定の永続化
* **内容:** 設定をTOML等で保存し、タスクトレイから起動できるようにする。
* **DoD:** PC起動時に設定をロードし、キャリブレーションなしですぐに指で操作を開始できる。

---

### 実装に向けたヒント
`ort` で手のランドマークを扱う場合、[GoogleのMediaPipe公式モデル (TFLite)](https://developers.google.com/mediapipe/solutions/vision/hand_landmarker) をONNXに変換したものを使うのが一般的です。

